<h1>Cards#edit</h1>
<p>Find me in app/views/cards/edit.html.erb</p>

<!-- <form action=<%= "/cards/#{@card.id}" %>, method='PATCH' data-remote="true">
  <input id="x" type="hidden" name="content">
  <trix-editor input="x"></trix-editor>
  <input type="submit" id="save">
</form>
 -->

<%= render 'card_form', world: @world, card: @card %>

<div class="text-muted">
  <% @card.taggings.each do |tagging| %>
    <div>
      <%= "##{tagging.tag.name.gsub(" ", "_")}" %>
    </div>
  <% end %>
</div>

<%= simple_form_for([@world, @card, @tagging]) do |f| %>
  <%= simple_fields_for :other do |o| %>
    <%= o.input :name, as: :string, label: 'tag' %>
  <% end %>
  <%= f.submit %>

<% end %>
