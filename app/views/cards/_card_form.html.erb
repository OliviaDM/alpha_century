<%= simple_form_for([@world, @card]) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.trix_editor :content %>

  <%= f.input :is_event, as: :boolean %>

  <%= f.submit %>

<% end %>
